{
  "name": "redis-if",
  "version": "0.0.0",
  "description": "Script for conditional transactions in Redis.",
  "keywords": [
    "redis",
    "transaction"
  ],
  "repository": "nodeca/redis-if",
  "license": "MIT",
  "files": [
    "index.js",
    "transaction.lua"
  ],
  "scripts": {
    "test": "standard && mocha"
  },
  "devDependencies": {
    "ioredis": "^4.22.0",
    "mocha": "^8.3.0",
    "standard": "^16.0.3"
  }
}
